# HAR MCP Server 依赖
# Python >= 3.10

# 官方 MCP Python SDK
mcp>=1.0.0

# 流式 JSON 解析，处理大体积 HAR 文件的核心依赖
ijson>=3.3.0

# ── 可选：脱敏模块依赖 ──────────────────────────────────
# sanitizer.py 支持多种脱敏源，按优先级自动选择：
# 1. Sensitive-lexicon + Aho-Corasick（推荐，需额外 git clone 词库）
# 2. better-profanity（英文敏感词过滤，备选）
# 3. alt-profanity-check（多语言敏感词检测，备选）
# 4. 本地 wordlist.txt 自定义词库（兜底）
# 若全部未安装，sanitizer 自动降级为空操作，不影响主流程。
pyahocorasick>=2.0.0
better-profanity>=0.7.0
alt-profanity-check>=1.0.0
